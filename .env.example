# ===== Graylog/OpenSearch Stack Settings =====
# Change these before production.
# Public URL where Graylog will be accessed (trailing slash required)
GRAYLOG_HTTP_EXTERNAL_URI=http://YOUR_SERVER_IP:9000/
# Timezone for Graylog
GRAYLOG_TIMEZONE=Asia/Dhaka

# Graylog secrets
# Generate a long random string (min 16 chars)
GL_PASSWORD_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
# SHA256 of admin password (default: admin)
# To set your own: echo -n 'yourpass' | sha256sum | cut -d' ' -f1
GL_ROOT_PW_SHA2=8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918

# Java heaps (tune for your server). For ~128GB RAM box:
# OpenSearch gets ~32G, Graylog 8G. Adjust as needed.
OPENSEARCH_JAVA_OPTS=-Xms16g -Xmx16g
GRAYLOG_JAVA_OPTS=-Xms4g -Xmx8g

# Storage paths (bind mounts). Make sure these have fast NVMe SSDs.
DATA_DIR=/data/di-logstack
MONGO_DB_PATH=${DATA_DIR}/mongo
OPENSEARCH_DATA_PATH=${DATA_DIR}/opensearch
GRAYLOG_DATA_PATH=${DATA_DIR}/graylog

# Ports
# Web: 9000; REST API also on 9000
# We will provision inputs to bind Syslog/GELF/NetFlow/IPFIX to these ports.
SYSLOG_UDP_PORT=1514
SYSLOG_TCP_PORT=1514
GELF_UDP_PORT=12201
GELF_TCP_PORT=12201
NETFLOW_PORT=2055
IPFIX_PORT=4739
BEATS_PORT=5044
